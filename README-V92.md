# MediaBay V92 — Unified Page Transition

Every HTML page now starts with the same MediaBay green loading screen used by the homepage. Internal HTML navigation no longer creates the previous dark overlay; the destination page presents the unified preloader, so changing page and opening the site use the same visual transition.

No page content or the working Brief/FormSubmit engine was intentionally changed. Scroll/motion systems remain as they were.
