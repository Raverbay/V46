async function initHome(){const ps=await(await fetch("products.json")).json();const h=document.getElementById("homeProducts");if(h)h.innerHTML=ps.slice(0,4).map(p=>`<a class="product-card" href="product.html?id=${p.id}"><div class="product-media"><span class="badge">${p.badge}</span><img src="${p.image}" alt="${p.brand} ${p.name}" loading="lazy"></div><div class="product-info"><span>${p.brand}</span><h3>${p.name}</h3><p>${money(p.price)}</p></div></a>`).join("");const brands=[...new Set(ps.map(p=>p.brand))].sort();const b=document.getElementById("brandList");if(b)b.innerHTML=brands.map(x=>`<a href="shop.html?brand=${encodeURIComponent(x)}"><span>${x}</span><b>↗</b></a>`).join("");const btn=document.getElementById("menuBtn"),menu=document.getElementById("mobileMenu");btn?.addEventListener("click",()=>{const open=btn.getAttribute("aria-expanded")==="true";btn.setAttribute("aria-expanded",String(!open));menu.setAttribute("aria-hidden",String(open));menu.classList.toggle("open",!open)});menu?.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>{btn?.setAttribute("aria-expanded","false");menu.classList.remove("open")}))}
document.addEventListener("DOMContentLoaded",initHome);